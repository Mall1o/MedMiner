export const toggleDropdown = (event) => {
    const dropdown = event.currentTarget.querySelector('.dropdown-menu');
    dropdown.classList.toggle('open');
};

// Puoi aggiungere qui altre logiche interattive, come animazioni o gestori di eventi.
